
#reader
DATA_PATH = 'data/'
TYPE = 'LOL_'
RUN_PATH = 'runs.txt'

#merge info
N_JSON_PROCESES = 4
PRE_MERGE_INFO = False
SHOW_STRG_INFO = False
ONLY_MERGE = False #Only merges input and storage info and saves the file
ONLY_REDUCE = False #Only reduces merged info to signatures with more than MIN_LEN_TSX and saves the file

#paralel execution
TRAIN_NEW = True #Deletes previous model info
#MIN_LEN_TSX = 10000
MIN_LEN_TSX = 100 #Change when working with all the data
MAX_LEN_TSX = 200000 #MaxGlobal is 2.375.861
MIN_LEN_KNN = 10
MAX_LEN_KNN = 600
DO_TRAIN_NOT_KNN = True #Trains the gas prediction models and not the KNN for storage info prediction
#If False set STRG_MANAGEMENT to 0 (if you don't prediction will be skewed)

#preprocess
STRG_MANAGEMENT = 0 #  0 = dump no storage info, 1 = all {}, 2 = use KNN
SAVE_HASH_MISSING_INFO = False